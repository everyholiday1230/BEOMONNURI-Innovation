node_modules/
dist/
build/
coverage/
.turbo/
*.log
.DS_Store
.env
.env.local
.env.*.local
playwright-report/
test-results/
*.tsbuildinfo

node_modules
dist
.turbo
coverage
test-results
playwright-report
tests/e2e/results.json

.data/
*.db
*.db-wal
*.db-shm
tests/e2e-admin/results.json

# Terraform — provider plugin cache and state must never be committed.
# `.terraform.lock.hcl` IS committed on purpose (provider checksum pinning).
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
terraform.tfvars

# IR 화면 캡처 — 재생성 가능(tools/ir-screenshots.mjs)하고 용량이 크므로 커밋하지 않는다.
docs/ir-screenshots/
