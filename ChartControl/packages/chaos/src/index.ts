// Chaos test harness (Phase 6 §10). Fault scenarios are reproduced with mocks/proxies — never against
// real production services. This module is test-only; the export exists to satisfy package resolution.
export const CHAOS_HARNESS = 'phase6-chaos';
