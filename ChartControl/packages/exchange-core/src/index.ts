/*
   거래소 중립 계약(contracts).

   ★★ 왜 별도 패키지인가

     이 정의들은 `@quantumtrade/exchange-bitmart` 안에 있었다. 그런데 **KuCoin 어댑터가
     이것을 가져다 쓴다** — 지금 실제로 거래를 처리하는 것은 KuCoin 이다. 즉 살아 있는
     거래 경로가 다른(연결도 되지 않는) 거래소 이름의 패키지를 의존하고 있었다.

     그래서 두 가지 오해가 생긴다:
       · BitMart 를 쓰지 않으니 그 패키지를 지워도 된다고 판단할 수 있다 —
         **지우면 거래가 멈춘다.**
       · 반대로 KuCoin 코드를 읽는 사람은 왜 BitMart 를 import 하는지 알 수 없다.

   ★ 여기에는 **어느 거래소에도 속하지 않는 것만** 둔다. 서명 방식, 요율 제한, 브로커
     리베이트처럼 거래소마다 다른 것은 각 거래소 패키지에 남는다.

   ★ 런타임 코드가 없다 — 전부 타입이다. 그래서 이 분리는 동작을 바꾸지 않는다.
*/
export * from './modes';
export * from './interfaces';
